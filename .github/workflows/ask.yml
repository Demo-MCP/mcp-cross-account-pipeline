name: Ask Infrastructure Question

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  ask:
    if: (github.event.issue.pull_request || github.event.pull_request) && contains(github.event.comment.body, '/ask')
    runs-on: self-hosted
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Extract question
      id: question
      run: |
        COMMENT="${{ github.event.comment.body }}"
        QUESTION=$(echo "$COMMENT" | sed 's|^/ask ||')
        echo "text=$QUESTION" >> $GITHUB_OUTPUT
        
    - name: Install Python dependencies
      run: |
        echo "Dependencies not needed - using HTTP broker service"
        
    - name: Call broker service
      id: broker
      run: |
        echo "Question: ${{ steps.question.outputs.text }}"
        echo "Calling broker service..."
        
        RESULT=$(curl -s -X POST \
          "http://internal-broker-internal-alb-572182136.us-east-1.elb.amazonaws.com/ask" \
          -H "Content-Type: application/json" \
          -d "{
            \"ask_text\": \"${{ steps.question.outputs.text }}\",
            \"account_id\": \"500330120558\",
            \"region\": \"us-east-1\",
            \"metadata\": {
              \"actor\": \"${{ github.actor }}\",
              \"repo\": \"${{ github.repository }}\",
              \"pr_number\": ${{ github.event.issue.number || github.event.pull_request.number }},
              \"run_id\": \"${{ github.run_id }}\"
            },
            \"shim_url\": \"http://internal-mcp-internal-alb-2059913293.us-east-1.elb.amazonaws.com\"
          }" \
          --max-time 120 \
          --fail-with-body)
        
        CURL_EXIT=$?
        if [ $CURL_EXIT -ne 0 ]; then
          echo "Curl failed with exit code: $CURL_EXIT"
          echo "Response: $RESULT"
          exit $CURL_EXIT
        fi
        
        echo "Broker response: $RESULT"
        
        # Extract answer from JSON response
        ANSWER=$(echo "$RESULT" | jq -r '.answer // "No answer field"' 2>/dev/null || echo "JSON parse failed: $RESULT")
        
        echo "result<<EOF" >> $GITHUB_OUTPUT
        echo "$ANSWER" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
    - name: Log broker result
      env:
        RESULT: ${{ steps.broker.outputs.result }}
      run: |
        echo "=== BROKER RESULT ==="
        echo "$RESULT"
        echo "===================="
